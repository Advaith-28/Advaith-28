
<!----- Creating PUBLISHER table (Parent of BOOK)
CREATE TABLE PUBLISHER (
    PUBLISHER_ID INT PRIMARY KEY,
    NAME VARCHAR(50),
    PUB_ADDRESS VARCHAR(50)
);

-- Inserting values into PUBLISHER table
INSERT INTO PUBLISHER VALUES
(152, 'Pearson', 'Japan');
INSERT INTO PUBLISHER VALUES
(159, 'Silver Shots', 'India');
INSERT INTO PUBLISHER VALUES
(157, 'Samantha', 'America');
INSERT INTO PUBLISHER VALUES
(160, 'APJ', 'India');
INSERT INTO PUBLISHER VALUES
(151, 'Jungoo', 'Korea');

-- Creating BOOK table (Dependent on PUBLISHER)
CREATE TABLE BOOK (
    BOOK_ID INT PRIMARY KEY,
    TITLE VARCHAR(50),
    AUTHOR VARCHAR(50),
    MRP INT,
    PUBLISHER_ID INT,
    PUBLISHER DATE,
    VOLUME INT,
    ISBN VARCHAR(20),
    FOREIGN KEY (PUBLISHER_ID) REFERENCES PUBLISHER(PUBLISHER_ID)
);

-- Inserting values into BOOK table
INSERT INTO BOOK VALUES
(125, 'Computer Org', 'Pearson', 150, 152, '25-DEC-06', 25, '125478963ASDE');
INSERT INTO BOOK VALUES
(127, 'Computer Data', 'Pearson', 152, 152, '25-JUN-16', 25, '125478963DSDE');
INSERT INTO BOOK VALUES
(128, 'Computer Hardware', 'Pearson', 125, 152, '25-JAN-20', 25, '125478963DSUE');
INSERT INTO BOOK VALUES
(129, 'Data Structures', 'Samantha', 125, 157, '25-FEB-16', 25, '125478963RSUE');
INSERT INTO BOOK VALUES
(126, 'DBMS', 'Silver Shots', 100, 159, '25-MAR-06', 25, '125478963DYUE');

-- Creating AUTHOR table
CREATE TABLE AUTHOR (
    AUTHOR_ID INT PRIMARY KEY,
    NAME VARCHAR(50),
    PHONE_NO VARCHAR(20),
    STATUS VARCHAR(10)
);

-- Inserting values into AUTHOR table
INSERT INTO AUTHOR VALUES
(1125, 'Pearson', '9874512360', 'Active');
INSERT INTO AUTHOR VALUES
(1126, 'Samantha', '9871712360', 'Active');
INSERT INTO AUTHOR VALUES
(1127, 'Pearson', '9991712360', 'Inactive');
INSERT INTO AUTHOR VALUES
(1128, 'Silver Shots', '9000712360', 'Active');
INSERT INTO AUTHOR VALUES
(1129, 'Silver Shots', '9000712360', 'Active');

-- Creating MEMBER table
CREATE TABLE MEMBER (
    MEMBER_ID INT PRIMARY KEY,
    NAME VARCHAR(50),
    BRANCH_CODE INT,
    ROLL_NO INT,
    PHONE_NO VARCHAR(20),
    EMAIL VARCHAR(50),
    DATE_OF_J DATE,
    STATUS VARCHAR(10)
);

-- Inserting values into MEMBER table
INSERT INTO MEMBER VALUES
(1000, 'John', 999, 300, '9856321047', 'john322@gmail.com', '15-JAN-21', 'Active');
INSERT INTO MEMBER VALUES
(1001, 'Johnson', 989, 400, '9870172360', 'johnson222@gmail.com', '20-JAN-21', 'Active');
INSERT INTO MEMBER VALUES
(1002, 'Nana', 989, 410, '980347047', 'nana100@gmail.com', '20-JUL-16', 'Inactive');
INSERT INTO MEMBER VALUES
(1003, 'Layla', 989, 420, '986317047', 'layla12@gmail.com', '20-MAR-17', 'Inactive');
INSERT INTO MEMBER VALUES
(1004, 'Miya', 999, 402, '9104340747', 'MIYA897@gmail.com', '20-OCT-21', 'Active');

-- Creating BOOK_ISSUE table (Dependent on BOOK and MEMBER)
CREATE TABLE BOOK_ISSUE (
    ISSUE_ID INT PRIMARY KEY,
    DATE_OF_I DATE,
    BOOK_ID INT,
    MEMBER_ID INT,
    EXP_DOR DATE,
    STATUS VARCHAR(10),
    FOREIGN KEY (BOOK_ID) REFERENCES BOOK(BOOK_ID),
    FOREIGN KEY (MEMBER_ID) REFERENCES MEMBER(MEMBER_ID)
);

-- Inserting values into BOOK_ISSUE table
INSERT INTO BOOK_ISSUE VALUES
(1256, '25-NOV-24', 126, 1004, '09-DEC-24', 'Returned');
INSERT INTO BOOK_ISSUE VALUES
(1258, '13-DEC-24', 128, 1002, '27-DEC-24', 'Returned');
INSERT INTO BOOK_ISSUE VALUES
(1255, '23-DEC-24', 125, 1004, '08-JAN-25', 'With');
INSERT INTO BOOK_ISSUE VALUES
(1257, '26-DEC-24', 127, 1003, '24-DEC-24', 'With');
INSERT INTO BOOK_ISSUE VALUES
(1259, '31-DEC-24', 129, 1001, '14-JAN-25', 'With');

-- Creating BOOK_AUTHOR table (Dependent on BOOK and AUTHOR)
CREATE TABLE BOOK_AUTHOR (
    BOOK_ID INT,
    AUTHOR_ID INT,
    PRIMARY KEY (BOOK_ID, AUTHOR_ID),
    FOREIGN KEY (BOOK_ID) REFERENCES BOOK(BOOK_ID),
    FOREIGN KEY (AUTHOR_ID) REFERENCES AUTHOR(AUTHOR_ID)
);

-- Inserting values into BOOK_AUTHOR table
INSERT INTO BOOK_AUTHOR VALUES
(125, 1125);
INSERT INTO BOOK_AUTHOR VALUES
(126, 1128);
INSERT INTO BOOK_AUTHOR VALUES
(127, 1125);
INSERT INTO BOOK_AUTHOR VALUES
(128, 1128);
INSERT INTO BOOK_AUTHOR VALUES
(129, 1129);

-- Creating BOOK_RETURN table (Dependent on BOOK_ISSUE)
CREATE TABLE BOOK_RETURN (
    ISSUE_ID INT PRIMARY KEY,
    ACTUAL_DA DATE,
    LATE_DAYS INT,
    LATE_FEES INT,
    FOREIGN KEY (ISSUE_ID) REFERENCES BOOK_ISSUE(ISSUE_ID)
);

-- Inserting values into BOOK_RETURN table
INSERT INTO BOOK_RETURN VALUES
(1257, '24-DEC-24', 10, 200);
INSERT INTO BOOK_RETURN VALUES
(1255, '08-JAN-25', 5, 100);
INSERT INTO BOOK_RETURN VALUES
(1259, '31-DEC-24', 3, 75);
INSERT INTO BOOK_RETURN VALUES
(1256, '09-DEC-24', 0, 0);
INSERT INTO BOOK_RETURN VALUES
(1258, '27-DEC-24', 1, 20);

-- Creating LATE_FEE_RULE table (No dependencies)
CREATE TABLE LATE_FEE_RULE (
    FROMDAYS INT,
    TO_DAYS INT,
    AMOUNT INT
);

-- Inserting values into LATE_FEE_RULE table
INSERT INTO LATE_FEE_RULE VALUES
(10, 0, 200);
INSERT INTO LATE_FEE_RULE VALUES
(5, 0, 100);
INSERT INTO LATE_FEE_RULE VALUES
(3, 0, 60);
INSERT INTO LATE_FEE_RULE VALUES
(1, 0, 20);
Advaith-28/Advaith-28 is a ✨ special ✨ repository because its `README.md` (this file) appears on your GitHub profile.
You can click the Preview link to take a look at your changes.
--->
